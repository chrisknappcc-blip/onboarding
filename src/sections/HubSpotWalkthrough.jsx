import ContentSection from './ContentSection.jsx';

export default function HubSpotWalkthrough({ trackKey }) {
  return <ContentSection trackKey={trackKey} section="hubspot-walkthrough" searchable emptyLabel="The guided HubSpot walkthrough is coming together - check back soon." />;
}
