import ContentSection from './ContentSection.jsx';

export default function GongLibrary({ trackKey }) {
  return <ContentSection trackKey={trackKey} section="gong-library" emptyLabel="Gong call links are being curated - check back soon." />;
}
