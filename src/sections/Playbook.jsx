import ContentSection from './ContentSection.jsx';

export default function Playbook({ trackKey }) {
  return <ContentSection trackKey={trackKey} section="playbook" emptyLabel="Playbook content is being finalized - check back soon." />;
}
