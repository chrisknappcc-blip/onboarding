import ContentSection from './ContentSection.jsx';

export default function Intranet({ trackKey }) {
  return <ContentSection trackKey={trackKey} section="intranet" emptyLabel="Intranet link from Isabel is pending." />;
}
