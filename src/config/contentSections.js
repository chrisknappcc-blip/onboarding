// Every section that lives in blob storage under content/{managerId}/{key}.json.
// Shared between the frontend (Content Library page) and the presets backend
// so a preset save/apply always covers the same complete set.
export const ALL_CONTENT_SECTIONS = [
  'intro-about',
  'intro-offerings',
  'intro-structure',
  'intro-team',
  'intro-network',
  'playbook',
  'gong-library',
  'app-walkthroughs',
  'intranet',
  'tasks'
];
